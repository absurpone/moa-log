<%= render "shared/header" %>
<%# 投稿一覧 %>
  <div class="new-post-contents">
    <h2 class="new-posts-title">お気に入り投稿一覧</h2>
    <ul class='post-lists'>
      <% @favorite_posts.each do |post| %>
        <%= render partial: "posts/post", locals: { post: post } %>
      <% end %>
    </ul>
  </div>
  <%# 投稿一覧 %>